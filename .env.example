# Default: sample adapter (no network required)
VITE_ADAPTER=sample

# HTTP adapter stub (uncomment when backend exists)
# VITE_ADAPTER=http
# VITE_HTTP_BASE_URL=http://localhost:8787
# VITE_HTTP_TOKEN=
